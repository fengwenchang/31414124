问题：
1.element-ui table加分页组件，切换pageSize时候表格抖动。
最后发现是样式影响，在el-table外层包裹一层div再加y轴自滚动就可以。
2.左侧导航栏想用v-for指令写，但是有的菜单又一级菜单，有的没有，于是里面再包裹一层el-menu但是发现active状态就隔离了
最后在el-menu里面用div元素循环就可以。
3.表单验证的时候rules的validator自定义校验函数里面this指向window导致无法去值用实例的方法和变量。
最后只能在data上面定义一个函数表达式，在validator中写函数变量次啊能解决this的问题

感想：
通过写这个系统更加熟悉了element-ui的相关组件还有遇见的坑，相信下次再遇到的时候不会阻塞在类似的问题上边。